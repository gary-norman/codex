# Environment Configuration Template
# Copy this to .env and modify for your environment

# Database environment (dev or prod)
DB_ENV=dev

# Database path
# - For local development (macOS/Linux): ./identifier.sqlite
# - For WSL: ./identifier.sqlite or /tmp/codex_dev.db
# - For production on ext4 (FUSE workaround): /var/lib/db-codex/dev_forum_database.db
DB_PATH=./identifier.sqlite

# Docker configuration (optional)
IMAGE=samuishark/codex-v1.0
CONTAINER=codex
PORT=8888

# IMPORTANT: SQLite and FUSE filesystems
# If your application code is on a FUSE filesystem (network mount, cloud storage, etc.),
# you MUST place the database on a native filesystem like ext4 to avoid locking issues.
# FUSE filesystems do not properly support SQLite's file locking mechanisms.
